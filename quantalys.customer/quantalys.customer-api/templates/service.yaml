apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantalys.customer.fullname" . }}
  labels:
    {{- include "quantalys.customer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantalys.customer.selectorLabels" . | nindent 4 }}
