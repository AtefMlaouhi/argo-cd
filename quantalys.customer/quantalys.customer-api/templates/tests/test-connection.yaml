apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quantalys.customer.fullname" . }}-test-connection"
  labels:
    {{- include "quantalys.customer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quantalys.customer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
