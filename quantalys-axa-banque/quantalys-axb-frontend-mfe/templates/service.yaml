apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantalys.axb.frontend.mfe.fullname" . }}
  labels:
    {{- include "quantalys.axb.frontend.mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantalys.axb.frontend.mfe.selectorLabels" . | nindent 4 }}
