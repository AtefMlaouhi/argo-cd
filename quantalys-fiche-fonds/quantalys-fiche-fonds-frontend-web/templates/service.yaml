apiVersion: v1
kind: Service
metadata:
  name: { { include "quantalys.fichefonds.mfe.fullname" . } }
  labels: { { - include "quantalys.fichefonds.mfe.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "quantalys.fichefonds.mfe.selectorLabels" . | nindent 4 } }
