apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantalys.policy.fullname" . }}
  labels:
    {{- include "quantalys.policy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantalys.policy.selectorLabels" . | nindent 4 }}
