apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quantalys.policy.fullname" . }}-test-connection"
  labels:
    {{- include "quantalys.policy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quantalys.policy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
