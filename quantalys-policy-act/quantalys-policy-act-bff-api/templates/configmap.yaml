kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "quantalys.policy.act.bff.fullname" . }}-config
data:
    appsettings-env.json: |
    {{- .Files.Get "settings/appsettings.json" | nindent 6 }}
