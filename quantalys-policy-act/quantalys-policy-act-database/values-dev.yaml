message-broker-namespace: message-broker-dev
pgadmin-namespace: o2sm-tooling-dev

postgresql:
  fullnameOverride: policy-act-postgresql
  primary:
    persistence:
      size: 1Gi

    resources:
      limits:
        memory: 1000Mi
        cpu: 1000m
      requests:
        memory: 1000Mi
        cpu: 1000m
