kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "quantalys.policy.act.policyholder.fullname" . }}-config
data:
    appsettings-env.json: |
    {{- toYaml .Values.appsettingsJson | nindent 6 }}
